# You are given a string .
# Your task is to find out if the string S contains any:
# alphanumeric characters, alphabetical characters, digits, lowercase and uppercase characters.

if __name__ == '__main__':
    s = input()
    print(any(True  if s.isalnum() else False for s in s))
    print(any(True  if s.isalpha() else False for s in s))
    print(any(True  if s.isdigit() else False for s in s))
    print(any(True  if s.islower() else False for s in s))
    print(any(True  if s.isupper() else False for s in s))
